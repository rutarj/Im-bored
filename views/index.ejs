<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/main.css"> <!-- Link to the CSS file for styling -->
  <title>I'm Bored</title> <!-- Page title -->
</head>

<body>
  <div class="container">
    <h1>Let's find something for you to do ✌️</h1> <!-- Main heading -->

    <form action="/" id="form" class="form" method="POST"> <!-- Form for user input -->
      <select name="type" class="form-select"> <!-- Dropdown for activity type -->
        <option value="" data-display="Select">Random type</option>
        <option value="education">Education</option>
        <option value="charity">Charity</option>
        <option value="recreational">Recreational</option>
        <option value="relaxation">Relaxation</option>
        <option value="busywork">Busywork</option>
        <option value="social">Social</option>
        <option value="diy">DIY</option>
        <option value="music">Music</option>
      </select>
      <select name="participants" class="form-select"> <!-- Dropdown for number of participants -->
        <option value="">Any number of people</option>
        <option value="1">1</option>
        <option value="2">2</option>
        <option value="3">3</option>
        <option value="4">4</option>
      </select>
      <button type="submit" class="form-submit">Go</button> <!-- Submit button -->
    </form>

    <section id="cards" class="cards"> <!-- Section to display activity results -->
      <% if (locals.data) { %> <!-- Check if there's data to display -->
        <article class="card-item"> <!-- Activity card -->
          <h2 class="card-activity"><%= data.activity %></h2> <!-- Display activity name -->
          <div class="card-info"> <!-- Info about the activity -->
            <span class="card-type"><%= data.type %></span> <!-- Display activity type -->
            <span class="card-participants">participants: <%= data.participants %></span> <!-- Display number of participants -->
          </div>
        </article>
      <% } %>

      <% if (locals.error) { %> <!-- Check if there's an error message to display -->
        <div id="tag-error" class="tag-error"><%= error %></div> <!-- Display error message -->
      <% } %>
    </section>
  </div>

</body>

</html>